#replace-job #hasToDo 
***
- [ ] overwrite manual `#replace-job`

TS = typescript
slet = servlet
***
### jsp CRUD operation
==Important for monthly evidence.==

`.redirect` <- method
`doGet()`,  `doPost`, `doDelete()`, `doPut()`

# AngularJS
AngularJS is a script. Angular is the framework.
### installed nodeJS
from: https://nodejs.org/en/download/current ;
- commands
	- `npm -v`
	- `npm install -g @angular/cli` <- for windows.

### Intro
vanilla JS is not reactive by default, 
reacts to state change. [state is the value of attribute] 
**typescript**, a strongly type version of JS.
	- TS first converts vanilla JS then 

> Angular postponed  
# JSP (again)
## Chapter 2:
slet is an interface. 
all jsp page converts to slet, except for fragments.

### slet interface
`requestDispatcher` = create request from server

`filter` is an interface. request processing, it filters request.
`FilterChain`,  `filterconfig` 

***
`ServletConfig`

###  slet class
`pg.54`


***
# HW
 `doPut`, `doDelete` .
# imp jsp
==JSF বাদ দরকার নাই==
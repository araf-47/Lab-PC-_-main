# Chapter 22:

***
`O(1), O(n), O(n2), O(log n), O(n log n), O(mn)`  <- 99% problem follows this pattern.
TABLE 22.3 Change of Growth Rates
### 22.1 Introduction

### 22.2 Measuring Algorithm Efficiency Using Big O Notation
>in case of nested loop [1st loop * nested loop = total loop count]. 

` O(n)` <- this is called order of `n`.

- more input but less operation = efficient algorithm.
	- perfect example -> **Binary search**.

- **Big O notation** = just a theory of magnitude.  more input but less operation.

order of 1 => `O(1)`.  any amount of input but operation is same.

best-case input
worst-case input

TABLE 22.1 Growth Rates
we state magnitude not number. So order of `n`.

constant ignore.

input consumes memory. more input take more space.

### 22.4.1 Analyzing Binary Search

### 22.4.2 Analyzing Selection Sort

***
###  Dynamic Programming
recursive take less memory than loop.


***
***
# Chapter 23: Sorting
min to max || max to min
goal of sorting : efficient algorithm practice.
**Selection sort** -> OG sorting method

### 23.2 Insertion Sort
[recall this from your head --Araf].

### 23.3 Bubble Sort

### 23.4 Merge Sort
~~না পারলে ও চলবে~~ 
but important for Job 
এর পরের গুলা আর জানার দরকার নাই

### 23.5 Quick Sort

### Heap sort
কোন দরকার নাই।  বাদ। 





***
***
# HW 
chapter 22 : all algorithm, starting from Fibonacci number to the end of the chapter.
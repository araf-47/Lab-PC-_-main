json = JS object notation.


***
### XML DTD
Document Type Definition.

### XML Validator
- validate xml using DTD with java.

### XML Schema


### XLink
create hyperlink in xml file.

### Ignore list
- XML server.


# JDBC
**service** is a running application..
**common protocol**, **common channel** needed for services to talk to each other.
**TNS** only communicates to sqlplus.

JDBC (DB communication library) sqlplus alternative. Oracle has ODBC. 
JDBC is a wrapper of ODBC but with java.

`DriverManager` -> 
- `ResultSet` -> SQL query result.
	- `.next` -> moves the iterator to the 1st row
	- `.get` -> 
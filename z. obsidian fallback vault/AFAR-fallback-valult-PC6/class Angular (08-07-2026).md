`- websocket - server and browser two way communication

### Data model class
### Creating component and template
- template : html structure.
	- it must support a component.
- create component : `ng g c header` `ng g c [the component name]`
- add component to `app.ts`

#### reactive 
on an event change it changes the state.

- [ ] learn about what directive is in typescript
`pg: 271`
expression, returns string equivalate result.

- Table 10-3. The Basic Built-in Angular Directives
	- `ngIf` 
	- `ngFor`: this is like for each 
		- `NgForOf`
	- `ngSwitch`


- one way event/data binding 
	- interpolation 
	- event binding 
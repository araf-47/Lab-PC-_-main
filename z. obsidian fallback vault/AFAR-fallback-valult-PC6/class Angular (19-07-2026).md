Dependency injection : dependency will made out side the class and it will be inject or pushed in the class.

- Do file read write.

component has lifecycle . as long as it is in the view it is alive.
## ~~CHAPTER 19~~ : Using and Creating Modules
==sir didn't teach it from the book==.
now a days modules are not needed because, modern angular has standalone component.

routing happened under module.
module is like a subsystem. It itself is an application.


## Angular router.

## Angular authentication
json web token
### interceptor
give token to each header.
=> all `pg` are `pdf.pg`.
***
# Angular
to make reactive web app.
`ng` must be executable , and it should be recognized by environment variable.
`.exe` `.bat` `.cmd`

= > Just installed Angular and Git (Install git first to avoid error.)

- `public` - everything inside it will be publicly accessible.
- `src` - our coding ground (99% time we will be here.).
- `node_modules` - not  a part of my software that doesn't need to be in the version control. All downloaded libraries by npm are inside this.

- `.gitignore` - 
- `.pettierrc` - A plugin config file.
- `angular.json` - angular project config file.
- `package-lock.json` - library information
- `package.json` - main config file for npm. it also has package version. it has instruction of all the necessary library
- extra info : `npm i` works in isolation, in the project folder only.
`tsconfig.json` - TS config file.

Angular supports hot reload. 

### Understanding the Project Structure
`pg. 233`

### Understanding the Source Code Folder
`assets/` - external assets 
`app.module.ts` is now `app.config.ts`

unlike others angular loads JS first then html 

#### app component 
for this `html` , `ts/js`.

#### `main.ts`
entry point of the application
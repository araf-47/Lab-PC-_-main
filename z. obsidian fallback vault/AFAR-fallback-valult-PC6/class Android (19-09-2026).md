- what is android ?
	- Linux based OS.

> Framework : a list of library solves a big problem.

- Android SDK (< it is like JDK). Google provides this for developer.

## Android SDK

- `build.gradle` 
- `app/src` <- we write code here.
- AndroidManifast
	- `<intent-filter> ` 
- each page (`.xml` - layout, `.java` file - activity) is an activity 
- think of `launcher` as a browser and `activity` as page.

# Learn them before next class
- what is `activity` in Android studio.
- `<intent-filter> ` 
- what is `intent` .
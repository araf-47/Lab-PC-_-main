DTO = data transfer object.

all logic with abstraction and all data with encapsulation.
- model - is just domain structure. / entity

@Entity  means a database table. it manages 
all logic goes inside service

JPA